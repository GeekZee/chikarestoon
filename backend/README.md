## Technologies
- FastAPI
- SQLModel orm


## Run

* clone this repo
* ``` cd backend ```
* ``` python -m venv venv  ```
* ``` source ./venv/bin/activate ```
* ``` pip install -r src/requirements.txt ```
* ``` uvicorn src.main:app --reload ```
